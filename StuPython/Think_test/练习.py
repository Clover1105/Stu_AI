# print("======================= 数字运算 =======================")
# # 1. **整数运算**
# #     编写程序，计算 `(25 + 38) * 4 / 2` 的结果，并打印输出。
# a = (25 + 38) * 4 / 2
# print(a)
# print("-*"*28)
#
# # 2. **取余与整除**
# #     输入一个三位数（如 `367`），输出它的百位数、十位数和个位数。
# #     *提示：使用整除 // 和取余 % 运算。*
# n = int(input("请输入一个三位数："))
# ge = n % 10
# shi = n // 10 % 10
# bai = n // 100
# print(f"个位数为：{ge}\t十位数为：{shi}\t百位数为：{bai}")
# print("-*"*28)
#
# # 3. **角度计算**
# #     已知直角三角形的两条直角边分别为 `3` 和 `4`，求斜边的长度（保留两位小数）。
# #     *提示：使用 math.sqrt() 或 **0.5。*
# a,b = 3,4
# c = (a**2 + b**2) ** 0.5
# print(f"斜边长度为：{c:.2f}")
# print("-*"*28)
#
# # 4. **数字进制转换**
# #     输入一个十进制整数，输出它的二进制、八进制和十六进制表示。
# #     *提示：使用 bin()、oct()、hex()。*
# i = int(input("请输入一个十进制整数："))
# print(f"十进制数{i}的二进制表示为：{bin(i)}")
# print(f"十进制数{i}的八进制表示为：{oct(i)}")
# print(f"十进制数{i}的十六进制表示为：{hex(i)}")



# print("\n\n======================= 字符串操作 =======================")
# # 1. **字符串拼接与格式化**
# #     定义三个变量 `name = "张三"`、`age = 18`、`city = "北京"`，使用 f-string 输出：
# #     `我是张三，今年18岁，来自北京。`
# name = "张三"
# age = 18
# city = "北京"
# print(f"我是{name}，今年{age}岁，来自{city}。")
# print("-*"*28)
#
# # 2. **字符串索引与切片**
# #     给定字符串 `s = "Python Programming"`，分别取出：
# #     - 第一个字符
# #     - 最后一个字符
# #     - 前5个字符
# #     - 从索引3开始到结尾的字符
# s = "Python Programming"
# print(f"第一个字符为：{s[0]}")
# print(f"最后一个字符为：{s[-1]}")
# print(f"前5个字符为：{s[:5]}")
# print(f"从索引3开始到结尾的字符为：{s[3:]}")
# print("-*"*28)
#
# # 3. **字符串常用方法**
# #     给定字符串 `" hello world "`，完成以下操作：
# #     - 去掉两端空格
# #     - 转换为全大写
# #     - 将 `world` 替换为 `Python`
# #     - 按空格分割成列表
# s = " hello world "
# print(s.strip())
# print(s.upper())
# print(s.replace("world","Python"))
# print(s.split())
# print("-*"*28)
#
# # 4. **查找子串**
# #     给定字符串 `"abcabcabc"`，找出第二个 `"abc"` 的起始索引。如果不存在则输出 `-1`。
# #     *提示：使用 find() 方法。*
# s = "abcabcabc"
# print(s.find("abc",2))



# print("\n\n======================= 输入与输出 =======================")
# # 1. **用户输入与类型转换**
# #     编写程序，接收用户输入的年龄，判断是否大于等于18岁，并输出对应的提示信息。
# age = int(input("请输入年龄："))
# if age >= 18:
#     print("恭喜你，已成年")
# else:
#     print("请继续等待")
# print("-*"*28)
#
# # 2. **格式化输出**
# #     接收用户输入的姓名和身高（米），输出：
# #     `姓名：xxx，身高：x.xx米`
# #     身高保留两位小数。
# name = input("请输入姓名：")
# height = float(input("请输入身高（米）："))
# print(f"姓名：{name}，身高：{height:.2f}米")



# print("\n\n======================= 列表操作 =======================")
# # 1. **列表索引与切片**
# #     给定列表 `nums = [10, 20, 30, 40, 50, 60, 70]`，完成：
# #
# #     - 输出第3个元素
# #     - 输出倒数第2个元素
# #     - 输出索引2到5的元素（不包含5）
# #     - 输出所有元素，步长为2
# nums = [10, 20, 30, 40, 50, 60, 70]
# print(f"输出第3个元素:{nums[2]}")
# print(f"输出倒数第2个元素:{nums[-2]}")
# print(f"输出索引2到5的元素（不包含5）：{nums[2:5]}")
# print(f"输出所有元素，步长为2：{nums[::2]}")
# print("-*"*28)
#
# # 2. **列表增删改**
# #     定义一个空列表，依次执行：
# #     - 添加 `10`, `20`, `30`
# #     - 在索引1处插入 `15`
# #     - 删除元素 `20`
# #     - 弹出最后一个元素
# #         每一步之后都打印当前列表。
# ls = []
# ls.append(10)
# ls.append(20)
# ls.append(30)
# print(f"添加10,20,30后：{ls}")
# ls.insert(1,15)
# print(f"在索引1处插入15后：{ls}")
# ls.remove(20)
# print(f"删除元素20后：{ls}")
# ls.pop()
# print(f"弹出最后一个元素后：{ls}")
# print("-*"*28)
#
# # 3. **列表统计与排序**
# #     给定列表 `scores = [88, 72, 93, 68, 95, 72, 88]`，完成：
# #     - 输出最高分、最低分
# #     - 输出 `72` 出现的次数
# #     - 对列表进行降序排序并输出
# scores = [88, 72, 93, 68, 95, 72, 88]
# print(f"最高分：{max(scores)}")
# print(f"最低分：{min(scores)}")
# print(f"72出现的次数：{scores.count(72)}")
# scores.sort(reverse=True)
# print(f"降序排序后：{scores}")
#
# # 4. **列表嵌套**
# #     定义列表 `matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]`，输出：
# #     - 第二行第一列的元素
# #     - 所有元素（遍历输出）
# matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
# print(f"第二行第一列的元素：{matrix[1][0]}")
# for i in matrix:
#     for j in i:
#         print(j,end=" ")
#
# # 5. **列表引用与复制**
# #     创建列表 `a = [1, 2, 3]`，将 `a` 赋值给 `b`，
# #     然后修改 `b[0] = 100`，观察并输出 `a` 和 `b`。
# #     再使用 `copy()` 方法实现浅拷贝，重复上述操作。
# a = [1, 2, 3]
# b = a
# b[0] = 100
# print(f"a:{a}")
# print(f"b:{b}")
# b = a.copy()
# b[0] = 100
# print(f"a:{a}")
# print(f"b:{b}")



# print("\n\n======================= 条件语句 =======================")
# # 1. **成绩等级判断**
# #     接收用户输入的成绩（0-100），输出对应等级：
# #     - 90分及以上：优秀
# #     - 80-89分：良好
# #     - 60-79分：及格
# #     - 60分以下：不及格
# score = int(input("请输入成绩："))
# if score >= 90:
#     print("优秀")
# elif score >= 80:
#     print("良好")
# elif score >= 60:
#     print("及格")
# else:
#     print("不及格")
# print("-*"*28)
#
# # 2. **闰年判断**
# #     接收用户输入的年份，判断是否为闰年。
# #     *闰年规则：能被4整除但不能被100整除，或者能被400整除。*
# year = int(input("请输入年份："))
# if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
#     print("闰年")
# else:
#     print("平年")
# print("-*"*28)
#
# # 3. **逻辑运算练习**
# #     编写程序，接收用户输入的两个整数，判断是否同时大于10且和为偶数。
# #     如果满足，输出 `"满足条件"`，否则输出 `"不满足"`。
# a = int(input("请输入第一个整数："))
# b = int(input("请输入第二个整数："))
# if a > 10 and b > 10 and (a + b) % 2 == 0:
#     print("满足条件")
# else:
#     print("不满足")



print("\n\n======================= 循环语句 =======================")
# 1. **for循环遍历**
#     给定列表 `numbers = [12, 35, 17, 8, 29, 42]`，使用 `for` 循环输出所有偶数。
numbers = [12, 35, 17, 8, 29, 42]
for i in numbers:
    if i % 2 == 0:
        print(i,end=" ")
print("\n"+"-*"*28)

# 2. **while循环应用**
#     使用 `while` 循环计算 `1 + 2 + 3 + ... + 100` 的和，并输出结果。
sum = 0
i = 1
while i<= 100:
    sum += i
    i += 1
print(sum)